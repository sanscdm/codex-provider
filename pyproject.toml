[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-provider"
version = "0.1.0"
description = "Safely switch Codex desktop model providers while keeping native CLI profiles."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Santiago Toro" }]
keywords = ["codex", "model-provider", "openai", "deepseek", "amazon-bedrock"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: MIT License",
  "Operating System :: MacOS",
  "Operating System :: POSIX",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]
dependencies = ["tomli>=2.0.1; python_version < '3.11'"]

[project.scripts]
codex-provider = "codex_provider.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
